<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                  xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
                  prism:ViewModelLocator.AutowireViewModel="True"
                  x:Class="Adopcat.Mobile.Views.MenuPage">
    <MasterDetailPage.Master>
        <ContentPage Title="Menu">
            <StackLayout VerticalOptions="FillAndExpand">
                <StackLayout BackgroundColor="#006df0"
                             Orientation="Horizontal"
                             HorizontalOptions="FillAndExpand"
                             VerticalOptions="Start">
                    <Image Source="{Binding User.PictureUrl}" 
                           HorizontalOptions="Center"
                           Aspect="Fill"
                           HeightRequest="50"
                           WidthRequest="50" />
                    <Label Text="{Binding User.Name}" 
                           HorizontalOptions="StartAndExpand"
                           VerticalTextAlignment="Center"
                           TextColor="White" />
                </StackLayout>
                <Label Text="Menu" FontSize="Medium" FontAttributes="Bold" />
                <ListView
                    VerticalOptions="FillAndExpand"
                    ItemsSource="{Binding MenuItems}"
                    ItemTapped="ListView_ItemTapped">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Orientation="Horizontal" Padding="10">
                                    <Image Source="{Binding Icon}" WidthRequest="16" HeightRequest="16" HorizontalOptions="Start"/>
                                    <Label Text="{Binding Text}" HorizontalOptions="StartAndExpand" VerticalTextAlignment="Center" />
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </ContentPage>
    </MasterDetailPage.Master>
</MasterDetailPage>