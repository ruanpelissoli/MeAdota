<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Adopcat.Mobile.Views.LoginDataPage"
             Title="{Binding Title}">
    <StackLayout 
         VerticalOptions="CenterAndExpand"
         HorizontalOptions="CenterAndExpand">
        <Entry Placeholder="E-mail" Text="{Binding Email}" />
        <Entry Placeholder="Senha" IsPassword="True" Text="{Binding Password}" />
        <Button Text="Login" Command="{Binding LoginCommand}"/>
        <Label Text="ou" HorizontalTextAlignment="Center" />
        <Button Text="Login com Facebook" Command="{Binding FacebookLoginCommand}" />
        <ActivityIndicator IsVisible="{Binding ShowLoading}" IsRunning="{Binding ShowLoading}" Color="Black" />
    </StackLayout>
</ContentPage>