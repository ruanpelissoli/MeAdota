<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Adopcat.Mobile.Views.LoginPage"
             Title="{Binding Title}">
    <RelativeLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <Image 
            VerticalOptions="FillAndExpand"
            HorizontalOptions="FillAndExpand"
            Aspect="AspectFill"
            Source="login_bg.jpg" 
            RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width}"
            RelativeLayout.HeightConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height}"/>
        <Grid 
            RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width}"
            RelativeLayout.HeightConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height}">
            <StackLayout 
                VerticalOptions="FillAndExpand"
                HorizontalOptions="CenterAndExpand">
                <StackLayout VerticalOptions="CenterAndExpand">
                    <Label Text="E-mail" Style="{StaticResource TitleLabel}" />
                    <Entry Text="{Binding Email}" BackgroundColor="White" Opacity="0.5" />
                    <Label Text="Senha" Style="{StaticResource TitleLabel}" />
                    <Entry IsPassword="True" Text="{Binding Password}" BackgroundColor="White" Opacity="0.5" />
                    <Button Text="Login" Command="{Binding LoginCommand}" Style="{StaticResource DefaultButton}" HorizontalOptions="FillAndExpand" />
                    <Label Text="ou" Style="{StaticResource TitleLabel}" HorizontalTextAlignment="Center" />
                    <Button Text="Login com Facebook" Command="{Binding FacebookLoginCommand}" Style="{StaticResource DefaultButton}" />
                    <ActivityIndicator IsVisible="{Binding ShowLoading}" IsRunning="{Binding ShowLoading}" Color="#00ad97" />
                </StackLayout>               
                <StackLayout VerticalOptions="End" HorizontalOptions="CenterAndExpand" Padding="10">
                    <Label Text="Cadastre-se" Style="{StaticResource TitleLabel}" FontSize="Medium">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer NumberOfTapsRequired="1" Command="{Binding RegisterCommand}"></TapGestureRecognizer>
                        </Label.GestureRecognizers>
                    </Label>
                </StackLayout>
            </StackLayout>
        </Grid>
    </RelativeLayout>
</ContentPage>