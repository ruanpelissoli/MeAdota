<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Adopcat.Mobile.Views.LoginPage"
             Title="{Binding Title}"
             BackgroundColor="White">
    <RelativeLayout>
        <Image 
            Aspect="AspectFit"
            Source="login_bg.jpg" 
            RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width}"
            RelativeLayout.HeightConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height}"/>
        <Grid 
            RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width}"
            RelativeLayout.HeightConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height}">
            <StackLayout 
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="CenterAndExpand">
                <Button Text="Login" Command="{Binding LoginCommand}" Style="{StaticResource DefaultButton}"/>
                <Label Text="ou" HorizontalTextAlignment="Center" />
                <Button Text="Casdastre-se" Command="{Binding RegisterCommand}" Style="{StaticResource DefaultButton}" />
            </StackLayout>
        </Grid>
    </RelativeLayout>   
</ContentPage>