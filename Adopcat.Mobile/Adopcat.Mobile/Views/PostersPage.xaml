<?xml version="1.0" encoding="utf-8" ?>
<CarouselPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             xmlns:converters="clr-namespace:Adopcat.Mobile.Util"
             xmlns:roundimage="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions"
             x:Class="Adopcat.Mobile.Views.PostersPage"
             Title="{Binding Title}"
             ItemsSource="{Binding Posters}"
             x:Name="MyPosterPage"
             Padding="20">
    <CarouselPage.ToolbarItems>
        <ToolbarItem Icon="icon_filter.png" Order="Primary" Command="{Binding FilterCommand}" />
    </CarouselPage.ToolbarItems>
    <CarouselPage.ItemTemplate>
        <DataTemplate>
            <ContentPage>
                <Grid VerticalOptions="FillAndExpand"
                      HorizontalOptions="FillAndExpand">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                    </Grid.ColumnDefinitions>
                    <Image x:Name="imgBackArrow" 
                           Source="icon_left_arrow.png" 
                           Grid.Column="0" 
                           VerticalOptions="Center" 
                           WidthRequest="20" 
                           HeightRequest="20"/>
                    <StackLayout 
                        Grid.Column="1"
                        Padding="20"
                        HorizontalOptions="CenterAndExpand"
                        VerticalOptions="FillAndExpand">
                        <Label Text="{Binding PetName}" 
                           FontAttributes="Bold" 
                           FontSize="Medium"
                           HorizontalTextAlignment="Center"/>
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer 
                           NumberOfTapsRequired="1" 
                           Command="{Binding Path=BindingContext.PosterSelectedCommand, Source={x:Reference MyPosterPage}}" 
                           CommandParameter="{Binding Id}" />
                        </StackLayout.GestureRecognizers>
                        <roundimage:CircleImage
                           Source="{Binding MainPictureUrl}"
                           Aspect="AspectFill"
                           HeightRequest="250" 
                           WidthRequest="250" />
                        <StackLayout Orientation="Horizontal">
                            <Label Text="Castrado" HorizontalOptions="StartAndExpand" FontSize="Small" />
                            <Image Source="icon_checked.png" HorizontalOptions="End" HeightRequest="20" WidthRequest="20" />
                        </StackLayout>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="Vermifugado" HorizontalOptions="StartAndExpand" FontSize="Small"/>
                            <Image Source="icon_checked.png" HorizontalOptions="End" HeightRequest="20" WidthRequest="20" />
                        </StackLayout>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="Entrega" HorizontalOptions="StartAndExpand" FontSize="Small"/>
                            <Image Source="icon_checked.png" HorizontalOptions="End" HeightRequest="20" WidthRequest="20" />
                        </StackLayout>
                        <Label Text="Cidade:" FontSize="Small"/>
                        <Label Text="{Binding City}" FontAttributes="Bold" FontSize="Small"/>
                    </StackLayout>
                    <Image x:Name="imgForwardArrow" 
                           Source="icon_right_arrow.png" 
                           Grid.Column="2" 
                           VerticalOptions="Center" 
                           WidthRequest="20" 
                           HeightRequest="20" />
                </Grid>
            </ContentPage>
        </DataTemplate>
    </CarouselPage.ItemTemplate>
</CarouselPage>