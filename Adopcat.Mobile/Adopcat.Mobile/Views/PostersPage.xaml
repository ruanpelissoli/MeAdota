<?xml version="1.0" encoding="utf-8" ?>
<CarouselPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             xmlns:converters="clr-namespace:Adopcat.Mobile.Util"
             x:Class="Adopcat.Mobile.Views.PostersPage"
             Title="{Binding Title}"
             ItemsSource="{Binding Posters}">
    <CarouselPage.ToolbarItems>
        <ToolbarItem Text="Filtro" Order="Primary" Command="{Binding FilterCommand}" />
    </CarouselPage.ToolbarItems>
    <CarouselPage.ItemTemplate>
        <DataTemplate>
            <ContentPage>
                <StackLayout 
                    HorizontalOptions="CenterAndExpand"
                    VerticalOptions="CenterAndExpand">
                    <Label Text="Nenhum anúncio encontrado!" IsVisible="{Binding IsEmpty}" />
                    <Image Source="{Binding PetPictures.Url}" />
                    <Label Text="{Binding Country}" />
                    <Label Text="{Binding State}" />
                    <Label Text="{Binding City}" />
                    <ActivityIndicator IsVisible="{Binding ShowLoading}" IsRunning="{Binding ShowLoading}" Color="Black" />
                </StackLayout>                
            </ContentPage>
        </DataTemplate>
    </CarouselPage.ItemTemplate>
</CarouselPage>